AUTOMAKE_OPTIONS = foreign

INSTALL_DIR  = $(includedir)

#install header files
install :
	@if [ ! -d $(INSTALL_DIR) ]; then mkdir -p $(INSTALL_DIR); fi
	@if [ ! -d $(INSTALL_DIR)/SchemaMgr ]; then mkdir -p $(INSTALL_DIR)/SchemaMgr; fi
#	@cp -u -f ./SchemaMgr/*.h  $(INSTALL_DIR)/SchemaMgr

